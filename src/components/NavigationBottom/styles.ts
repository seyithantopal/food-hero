import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  wrapper: {
    // backgroundColor: 'red',
    marginTop: 15,
  },
  menu: {
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    padding: 10,
  },
});
